<!--pages/user/index.wxml-->
<view class="app-container">
  <custom-nav-bar placeholder="{{true}}" customStyle="background: rgba(255, 255, 255, 0)" showSlot="{{true}}">
    <view class="navbar">自定义navbar</view>
  </custom-nav-bar>
  <view class="content">
    <view wx:for="{{10}}" wx:key="index" style="height: 200rpx; padding-bottom: 40rpx"
      >更多属性请查看custom-nav-bar组件-{{index}}</view
    >
  </view>
</view>
